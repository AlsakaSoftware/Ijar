import Foundation

struct UserDefaultsKeys {
    /// Track if user has triggered automatic search for their first query
    static let hasTriggeredFirstQuerySearch = "has_triggered_first_query_search"
}
