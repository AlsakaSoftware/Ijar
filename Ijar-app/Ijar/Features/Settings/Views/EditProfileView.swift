import SwiftUI

struct EditProfileView: View {
    var body: some View {
        VStack {
            Text("Edit Profile")
                .font(.largeTitle)
        }
        .navigationBarTitleDisplayMode(.inline)
    }
}