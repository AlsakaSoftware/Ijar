import SwiftUI

struct PreferencesView: View {
    var body: some View {
        VStack {
            Text("Preferences")
                .font(.largeTitle)
        }
        .navigationBarTitleDisplayMode(.inline)
    }
}